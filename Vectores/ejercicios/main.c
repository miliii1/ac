#include <stdio.h>
#include <stdlib.h>

/*
// 1) Dado un vector y su largo hacer un algoritmo que invierta el orden de los elementos. Implementar como función.

void invertirVector(int vector[], int largo) {
    int inicio = 0;
    int fin = largo - 1;
    int temp;

    while (inicio < fin) {
        temp = vector[inicio];
        vector[inicio] = vector[fin];
        vector[fin] = temp;
        inicio++;
        fin--;
    }
}

int main() {
    int vector[] = {1, 2, 3, 4, 5};
    int largo = sizeof(vector) / sizeof(vector[0]);

    printf("Vector original:\n");
    for (int i = 0; i < largo; i++) {
        printf("%d ", vector[i]);
    }

    invertirVector(vector, largo);

    printf("\nVector invertido:\n");
    for (int i = 0; i < largo; i++) {
        printf("%d ", vector[i]);
    }

    return 0;
}
*/
